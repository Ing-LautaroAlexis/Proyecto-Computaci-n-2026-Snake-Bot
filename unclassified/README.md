# Archivos sin etapa asignada

`botv6.py` y `botv6.1.py` se conservan intactos. Sus nombres y funciones indican
una etapa adicional que no aparece en la secuencia solicitada. No se presentan
como V5.5 ni como versiones ChatGPT.

Los originales `data` e `import asyncio.py` se conservan solo en el workspace,
excluidos mediante `.gitignore`, porque contienen credenciales incrustadas.
No se publican ni se modifican para ocultar su procedencia.
